
name: Testing workflow

on: push

jobs:
    test-job:
        runs-on: ubuntu-latest

        steps: 
            - name: Greetings
              run: echo "Praise The Lord"

            - name: Display the date
              run: date   




name: Multi-line checkout repo

on: workflow_dispatch

jobs:
    display:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout repo
              uses: actions/checkout@v4

            - name: display file content
              run: |
                ls -lrth
                cat README.md
                pwd